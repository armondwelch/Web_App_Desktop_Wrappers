/**
 * Renderer process for Evernote wrapper
 * Since we're loading an external web app (Evernote), we don't need any custom renderer code.
 * This file is kept minimal to avoid interfering with the Evernote web app.
 */

// No custom code needed - the Evernote web app handles everything
